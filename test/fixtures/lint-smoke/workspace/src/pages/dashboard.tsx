export default function DashboardPage(): JSX.Element {
    return (
        <main>
            <h1>Dashboard fixture</h1>
            <a href="/dashboard/settings">Settings</a>
        </main>
    );
}
