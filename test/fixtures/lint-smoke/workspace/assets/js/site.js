/**
 * Format a fixture label.
 *
 * @param {string} value - The raw label.
 *
 * @returns {string} The normalized label.
 */
export function formatLabel(value) {
    return value.trim().toUpperCase();
}
